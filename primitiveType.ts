// khai báo kiểu number cho biến test
let test: number = 100 
// test = 'message' // lỗi do gán biến test bằng string

// khai báo kiểu string cho biến
let message: string = "Hello"

// khai báo type boolean
let isOld: boolean = false

// type null
let empty: null = null
console.log(empty);

// type undefined
let text: undefined
console.log(text);

// union type
let mix: number | string = 'text'
mix = 1
console.log(mix);